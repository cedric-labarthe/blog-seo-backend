import { User, UserInput } from './user.entity'
import { Article, ArticleInput } from './article.entity'
import { Media, MediaInput, MediaType } from './media.entity'

export { User, UserInput }
export { Article, ArticleInput }
export { Media, MediaInput, MediaType }

export default [User, Article, Media]
